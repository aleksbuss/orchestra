{
  "id": "91-multi-constraint-refactor",
  "description": "Refactor preserving 5 explicit constraints simultaneously.",
  "tags": [
    "agentic",
    "multi-constraint",
    "honest-test",
    "selection-ab"
  ],
  "input": {
    "message": "Refactor this function:\n\ndef p(d):\n    r=[]\n    for k in d:\n        if d[k]>0:\n            r.append(k.upper())\n    return r\n\nRequirements, ALL must hold:\n1. Rename it to `positive_keys_upper`.\n2. Add full type hints (parameter and return).\n3. Use a list comprehension instead of the loop.\n4. Add a docstring explaining the behaviour.\n5. Preserve EXACT behaviour including that it only includes keys whose value is strictly greater than zero.\nOutput one Python code block.",
    "swarmEnabled": true,
    "forceSwarm": true
  },
  "assertions": [
    {
      "type": "contains",
      "value": "def positive_keys_upper"
    },
    {
      "type": "matches",
      "pattern": "->\\s*[Ll]ist|->\\s*list"
    },
    {
      "type": "matches",
      "pattern": "\\[.*for .* in .*if .*\\]"
    },
    {
      "type": "matches",
      "pattern": "\"\"\"|'''"
    },
    {
      "type": "matches",
      "pattern": ">\\s*0"
    }
  ]
}
