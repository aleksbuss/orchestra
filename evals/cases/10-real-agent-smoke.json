{
  "id": "10-real-agent-smoke",
  "description": "Smoke case for `npm run evals -- --real`: no mock_response, requires actual agent invocation. Asserts the response is a real sentence (not empty, not a single-letter glitch, contains a period or natural sentence shape).",
  "tags": ["smoke", "real-agent"],
  "input": {
    "message": "Reply with exactly one short sentence confirming you understood this evaluation prompt.",
    "swarmEnabled": false
  },
  "assertions": [
    {
      "type": "matches",
      "pattern": "\\b\\w+\\b.*\\b\\w+\\b.*\\b\\w+\\b",
      "flags": "i"
    },
    {
      "type": "matches",
      "pattern": "(understood|confirm|received|got it|acknowledge|yes)",
      "flags": "i"
    }
  ]
}
